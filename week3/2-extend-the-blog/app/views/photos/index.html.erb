<%= render '/back_button' %>

<h3>Photos</h3>

<% if @photos.present? %>
    <% @photos.each do |post| %>
        <%= render partial: '/post_list', locals: { post: post, post_id: post.id } %>
    <% end %>
<% else %>
    <p>No photos exist yet.</p>
<% end %>